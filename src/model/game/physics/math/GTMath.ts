import { Angle as GTAngle } from "./Angle";
import { Distance as GTDistance } from "./Distance";
import { Normal as GTNormal } from "./Normal";

export namespace GTMath {
  export const Normal = GTNormal;
  export const Angle = GTAngle;
  export const Distance = GTDistance;
}
